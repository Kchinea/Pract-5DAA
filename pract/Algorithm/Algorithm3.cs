namespace Pract5DAA.Algorithm;

public class Algorithm3 : IAlgorithm {
  public void Solve(Instance instance) {
    // Vehicle? selectedVehicle = null;
    // double bestInsertionCost = double.PositiveInfinity;

    // foreach (var vehicle in instance.Vehicles) {
    //   double cost = ComputeBestInsertionCost(vehicle, instance.Task);

    //   if (cost < bestInsertionCost) {
    //     selectedVehicle = vehicle;
    //     bestInsertionCost = cost;
    //   }
    // }

    Console.WriteLine($"Selected vehicle: None"); //{selectedVehicle?.Id ?? "None"}");
  }
}